import React from "react";
import styled from "styled-components";

const DropDown = ({name, color}) => {
  return (
    <div>
      {console.log(name, color)}
    </div>
  )
};

export default DropDown

const Container = styled.div`
  
`;
